// Compass OS Configuration
// Change goLiveDate from null to an ISO date string (e.g., "2026-04-15") to start the countdown.

export const compassConfig = {
  /** Set to null to hold at Day 1. Set to an ISO date string to start counting. */
  goLiveDate: null as string | null,
  totalDays: 100,
};
